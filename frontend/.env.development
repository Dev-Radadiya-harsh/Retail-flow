# Local dev only (npm run dev). Vite proxies /api → http://localhost:3001 (see vite.config.js).
# Production builds use frontend/.env.production (e.g. Render) — this file is ignored by vite build.
VITE_API_BASE_URL=/api
