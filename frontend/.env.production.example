# Copy to .env.production before `npm run build` (Firebase Hosting).
# Use your real Render API URL — must end with /api (same path prefix as local dev).

VITE_API_BASE_URL=https://YOUR-SERVICE-NAME.onrender.com/api
