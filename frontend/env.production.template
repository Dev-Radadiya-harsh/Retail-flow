# For Firebase Hosting: copy to .env.production OR run from repo root:
#   npm run tunnel:write     (writes .env.production with https://….loca.lt/api)
#
# Must be https:// and must NOT be onrender.com unless you still use Render.
# Example after tunnel:
# VITE_API_BASE_URL=https://your-subdomain.loca.lt/api

VITE_API_BASE_URL=https://YOUR-TUNNEL-HOST/api
